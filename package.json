{
  "name": "wastewise-mern",
  "version": "1.0.0",
  "private": true,
  "description": "WasteWise MERN stack waste reporting and management platform",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
